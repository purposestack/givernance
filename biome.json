{
  "$schema": "https://biomejs.dev/schemas/2.4.15/schema.json",
  "assist": {
    "enabled": true,
    "actions": {
      "source": {
        "organizeImports": "on"
      }
    }
  },
  "linter": {
    "enabled": true,
    "rules": {
      "recommended": true,
      "suspicious": {
        "noExplicitAny": "error",
        "noConsole": {
          "level": "warn",
          "options": {
            "allow": ["warn", "error"]
          }
        }
      },
      "complexity": {
        "noExcessiveCognitiveComplexity": {
          "level": "warn",
          "options": {
            "maxAllowedComplexity": 20
          }
        },
        "useLiteralKeys": "off"
      }
    }
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 100
  },
  "javascript": {
    "formatter": {
      "quoteStyle": "double",
      "semicolons": "always",
      "trailingCommas": "all",
      "arrowParentheses": "always"
    }
  },
  "overrides": [
    {
      "includes": ["**/*.test.ts", "**/*.test.tsx", "**/tests/**/*.ts", "**/tests/**/*.tsx"],
      "linter": {
        "rules": {
          "performance": {
            "noImgElement": "off"
          },
          "style": {
            "noNonNullAssertion": "off"
          }
        }
      }
    },
    {
      "includes": ["packages/web/src/**/*.ts", "packages/web/src/**/*.tsx"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "@givernance/shared": "Importing the @givernance/shared root barrel in packages/web is forbidden (ADR-013). Use a specific subpath like @givernance/shared/validators or @givernance/shared/constants.",
                  "@givernance/shared/schema": "Importing Drizzle schema in packages/web is forbidden (ADR-013). Use @/models/ for API response types.",
                  "@givernance/shared/events": "Importing domain events in packages/web is forbidden (ADR-013). Events are backend-only.",
                  "@givernance/shared/jobs": "Importing job definitions in packages/web is forbidden (ADR-013). Jobs are backend-only.",
                  "@givernance/shared/lib/receipt-crypto": "Importing lib/receipt-crypto in packages/web is forbidden (ADR-013). It uses node:crypto streams — server-only; receipts reach the browser via signed download URLs.",
                  "@givernance/shared/lib/s3-branding": "Importing lib/s3-branding in packages/web is forbidden (ADR-013). It pulls @aws-sdk/client-s3 + node:stream into the bundle — server-only; branding assets are public-read URLs.",
                  "@givernance/shared/lib/svg-sanitiser": "Importing lib/svg-sanitiser in packages/web is forbidden (ADR-013). It depends on jsdom (Node-only); sanitisation happens API-side at upload time.",
                  "@givernance/shared/lib/trace-context": "Importing lib/trace-context in packages/web is forbidden (ADR-013). W3C traceparent generation uses node:crypto — backend-only outbox plumbing.",
                  "@givernance/shared/signup": "Importing signup in packages/web is forbidden (ADR-013). It runtime-imports the Drizzle schema and server-side signup-recovery logic — call the API instead.",
                  "@givernance/shared/finance/reporting": "Importing finance/reporting in packages/web is forbidden (ADR-013). Server-only super-admin aggregation — it runtime-imports the Drizzle schema and drizzle-orm; consume /v1/superadmin/finance/* instead."
                }
              }
            }
          }
        }
      }
    }
  ],
  "files": {
    "includes": ["packages/*/src/**/*.ts", "packages/*/src/**/*.tsx"],
    "ignoreUnknown": true
  }
}
